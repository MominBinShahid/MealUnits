# 4a. This file is INERT where it currently ships, and it is here on purpose.
#
# Crawlers read robots.txt only from the root of a host. This app is served
# from a project path, so the file they actually obey is the apex one at
# mominbinshahid.github.io/robots.txt, which belongs to a different repository
# and already says "Allow: /". This copy, at /MealUnits/robots.txt, is read by
# nobody today.
#
# RULED BY MOMIN 2026-09-14: ship it anyway. The reason is portability, not
# crawling. Move this repository to its own domain and the file is already
# correct; leave it out and the move silently loses a directive nobody
# remembers was being inherited from a parent that no longer exists. A project
# that depends on a file in another repository has a dependency it cannot see.

User-agent: *
Allow: /

Sitemap: https://mominbinshahid.github.io/MealUnits/sitemap.xml
